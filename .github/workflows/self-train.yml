name: self-train

# Autoentrenamiento SEMANAL: el bot aprende de los últimos 7 días, perfecciona sus
# setups y ajusta la exigencia. Corre cada lunes (mejora rápido).
on:
  schedule:
    - cron: '30 13 * * 1' # cada lunes, 13:30 UTC
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install --omit=dev

      - name: Autoentrenamiento
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: node scripts/self-train.js

      - name: Guardar lo aprendido
        run: |
          git config user.name "autobot"
          git config user.email "autobot@users.noreply.github.com"
          git add cerebro-trading/00-principios cerebro-trading/.train-state.json 2>/dev/null || true
          if git diff --cached --quiet; then
            echo "Sin cambios."
          else
            git commit -m "self-train: autoentrenamiento mensual [skip ci]"
            git pull --rebase --autostash || true
            git push || true
          fi
